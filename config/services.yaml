services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    # ---------------------------------------------------------
    # Actions
    # ---------------------------------------------------------
    Ntriga\PimcoreAssetThumbnails\Action\GenerateImageThumbnailsAction:

    # ---------------------------------------------------------
    # Message Handlers
    # ---------------------------------------------------------
    Ntriga\PimcoreAssetThumbnails\MessageHandler\GenerateImageThumbnailsMessageHandler: ~
    Ntriga\PimcoreAssetThumbnails\MessageHandler\GenerateSingleImageThumbnailMessageHandler: ~
